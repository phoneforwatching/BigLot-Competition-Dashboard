[project]
name = "bridge-biglot"
version = "0.1.0"
description = "Bridge service for BigLot Competition"
requires-python = ">=3.9"
dependencies = [
    "MetaTrader5; sys_platform == 'win32'",
    "supabase",
    "python-dotenv",
    "requests",
    "pandas",
]
